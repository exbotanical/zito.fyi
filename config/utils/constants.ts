/**
 * @summary Site-wide constants
 */
export const constants = {
	/**
	 * @summary Posts per virtual Feed page
	 */
	postsPerFeedPage: 5,

	/**
	 * @summary Directory in which to house page metadata
	 */
	feedMetaDirectory: 'feed_meta',

	/**
	 * @summary Directory in which JSON static assets are allocated
	 */
	baseMetaDirectory: 'public'
};
