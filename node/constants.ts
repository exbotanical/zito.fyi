/**
 * Site-wide constants
 */
export const constants = {
  /**
   * Directory in which JSON static assets are allocated
   */
  baseMetaDirectory: 'public',

  /**
   * Directory in which to house page metadata
   */
  feedMetaDirectory: 'feed_meta',

  /**
   * Posts per virtual Feed page
   */
  postsPerFeedPage: 5
}
